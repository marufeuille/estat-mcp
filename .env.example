ESTAT_APP_ID=your_app_id_here

# 認証モード: "keycloak" (JWT検証) or "mock" (X-Roleヘッダー)
AUTH_MODE=mock

# Keycloak設定（AUTH_MODE=keycloak 時に使用）
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=estat
KEYCLOAK_AUDIENCE=estat-mcp-client

# JWKS公開鍵キャッシュTTL（秒）
JWKS_CACHE_TTL=300
